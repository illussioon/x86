# Visual check

The rendered architecture diagram is a 3120x896 PNG with readable labels for application, console, public API, resource loader, images, saved state, bootloader, MachineConfig, ExecutionBackend, CPU backend, devices/memory and platform adapters.

The terminal screenshot is a 1680x900 dark native-terminal composition with clear command/output hierarchy, readable monospace text, and the expected state/bootloader/checksum flow. It is linked from the English, Russian and Ukrainian console pages.
